import * as types from '../../mutation-types'

const state = {
    items: []
}

const mutations = {
    [types.EXPAND_MENU](state, menuItem) {
        // menu的mutation
    }
}

export default {
    state,
    mutations
}